Name: general case
Pre: ((C != 1) && !isSignBit(C))
%a = sdiv i11 %X, C
%r = sub 0, %a
  =>
%c1 = sub 0, C
%r = sdiv %X, %c1


Name: specific case
Pre: isSignBit(C)
%r = sdiv %X, C
  =>
%cmp = icmp eq %X, C
%r = zext %cmp
